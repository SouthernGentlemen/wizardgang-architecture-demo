import type { DemoDefinition } from '../types';

const demo: DemoDefinition = {
  "id": "graphql",
  "route": "/api/graphql",
  "title": "GraphQL",
  "group": "Integration",
  "sourcePath": "src/demos/graphql.ts",
  "summary": "Schema-driven application interface using the same security, validation, and persistence boundaries as REST.",
  "proves": [
    "Dedicated GraphQL exhibit route",
    "Shared Worker boundary",
    "D1-backed resolver target scaffold"
  ],
  "status": "scaffolded"
};

export default demo;
