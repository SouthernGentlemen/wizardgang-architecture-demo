/**
 * The generic demonstration endpoints were retired with the flattened page renderer.
 * This module remains source-compatible with the pre-flatten route manifest until the
 * registry and generated route artifacts are updated in the next controlled change.
 */
export {};
