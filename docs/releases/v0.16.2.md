# WizardGang Architecture Demo v0.16.2

**Product:** WizardGang Architecture Demo

**Version:** v0.16.2

**Release date:** 2026-09-09

**Commit:** the commit this tag names — `git rev-list -n1 v0.16.2`

## Scope

Reframe the Cloudflare Platform laboratory as a compact tabbed surface while
keeping each live API demonstration directly runnable.

## Included changes

- DEMO-211: promote Edge, Workers, Durable Objects, D1, and R2 into a flat,
  full-width tab strip with active-route styling and reduced top whitespace.
- DEMO-211: move Public source and Route source attribution into the shared
  footer and remove WG-ARCH identifiers, References, source panels, and
  Implementation notes from Platform pages.
- DEMO-209: retain the generated-script escaping fix that restores every shared
  `Run it` control.

## Validation

The change PR passed the complete repository check with 497 tests, local
migrations, the dependency audit with zero vulnerabilities, the Worker dry-run
build, and `git diff --check`. The exact annotated tag must reproduce `npm ci`,
`npm run check`, `npm run validate:migrations`, `npm run security:dependencies`,
and `npm run build` before deployment.

## Deployment

Target: `demo.wizardgang.ai`. The tag-triggered Release workflow reproduces the
annotated v0.16.2 state, publishes a release-bound assurance snapshot, deploys
the exact tag, and verifies public version/commit identity and Worker health.
Live acceptance additionally verifies the Platform tab layout and that a shared
Edge action returns and renders its API response without a browser syntax error.

## Known limitations

- Provider billing availability is independent of application health; unavailable billing is never estimated as real cost.
- Availability is measured observation history, not an SLA.
- Acceptance is not accessibility certification or an exhaustive cross-browser test.

## Previous release

v0.16.1

## Rollback target

v0.16.1
