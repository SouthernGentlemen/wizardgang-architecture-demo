# WizardGang Architecture Demo v0.16.9

**Product:** WizardGang Architecture Demo

**Version:** v0.16.9

**Release date:** 2026-09-09

**Commit:** the commit this tag names — `git rev-list -n1 v0.16.9`

## Scope

Complete the live Cloudflare Durable Objects analytics query by selecting the
storage date dimension required by provider-side date ordering.

## Included changes

- DEMO-227: select `dimensions { date }` for the ordered Durable Objects
  storage observation.
- DEMO-227: retain namespace-specific storage scoping and add a regression
  guard for the complete provider query shape.

## Validation

The exact combined GraphQL query was accepted by the live Cloudflare schema.
The release candidate passed the complete repository check with 505 tests,
local migrations, the dependency audit with zero vulnerabilities, the Worker
dry-run build, and `git diff --check`. The exact annotated tag must reproduce
the same gates before deployment.

## Deployment

Target: `demo.wizardgang.ai`. The tag-triggered Release workflow reproduces the
annotated v0.16.9 state, publishes a release-bound assurance snapshot, deploys
the exact tag, and verifies public version/commit identity and Worker health.
Live acceptance verifies that all 15 Workers, D1, R2, and Durable Objects
resource observations are available on `/operations/usage` and its shared
reporting interface.

## Known limitations

- Cloudflare's provider billing API does not currently return a usable billed
  cost value for this account; the page reports that field as unavailable and
  does not substitute a local estimate.
- Availability is measured observation history, not an SLA.
- Acceptance is not accessibility certification or an exhaustive cross-browser test.

## Previous release

v0.16.8

## Rollback target

v0.16.8
