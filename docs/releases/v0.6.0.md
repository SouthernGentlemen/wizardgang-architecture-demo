# WizardGang Architecture Demo v0.6.0

**Product:** WizardGang Architecture Demo

**Version:** v0.6.0

**Release date:** 2026-09-01

**Commit:** the commit this tag names — `git rev-list -n1 v0.6.0`

## Scope

Unify the complete architecture laboratory with the flagship `wizardgang.ai` brand system while preserving all public routes, executable demonstrations, authorization boundaries, offline behavior, and operational controls.

## Included changes

- [DEMO-046] [OPS] Record the verified v0.5.0 deployment
- [DEMO-047] [TEST] Make session tampering test deterministic
- [DEMO-048] [FEAT] Align the demo with WizardGang branding
- [DEMO-049] [BUILD] Record and version the v0.6.0 release

## Validation

- Generated 17-route HTML registry and 60-entry full route-contract validation
- Controlled history and pull-request title validation
- TypeScript typecheck, contract, six-locale, security, and local migration validation
- 74 automated tests across 17 files
- Dependency audit with no vulnerabilities
- Production Worker build at 3,575.65 KiB compressed
- Desktop browser verification in both branded dark and light themes
- Representative dense API-route verification with no horizontal overflow
- Responsive verification at a 390 × 844 viewport with no horizontal overflow
- Exact 1200 × 630 social preview image and offline-reachable asset response verification

## Deployment

Target: `demo.wizardgang.ai`

The release workflow reproduces this annotated tag, publishes these notes, confirms pending D1 migrations, deploys the exact tagged Worker source with its release identity, and verifies `/version` and `/health` against the tag commit.

## Known limitations

- Visitor write laboratories require the managed session secret and enforce per-session quotas and 24-hour expiry.
- Branch-protection evidence is reported as unverifiable when GitHub does not expose it anonymously and no optional read token is configured.
- Real GitHub webhook delivery requires environment-owned repository webhook configuration and a matching managed secret.
- Identity examples are provider-neutral boundaries, not a configured external tenant.
- WCAG and ISO references are alignment demonstrations, not certification claims.
- Billing data is synthetic.
- R2 uploads are limited to 10 objects, 5 MiB per object, and 20 MiB total per visitor session.
- The locally bundled GraphiQL runtime and social preview image increase the Worker bundle but remain within the deployment limit.

## Previous release

v0.5.0

## Rollback target

v0.5.0
