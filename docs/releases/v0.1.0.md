# WizardGang Architecture Demo v0.1.0

**Product:** WizardGang Architecture Demo

**Version:** v0.1.0

**Release date:** 2026-08-31

**Commit:** `9d6efb8841fe450d9ee094224d33eb529a460b06`

## Scope

First reviewed public architecture-laboratory baseline.

## Included changes

- DEMO-001 through DEMO-011: repository foundation, security and data boundaries, Cloudflare runtime primitives, controlled integration and identity interfaces, international and accessible UI behavior, operations and governance evidence, delivery controls, production bindings, and the release record.

## Validation

Run at the reconstructed release tree:

- PASS `npm ci`
- PASS `npm run check` — 49 tests across 12 files
- PASS `npm run validate:migrations`
- PASS `npm run security:dependencies` — no vulnerabilities
- PASS `npm run build`

## Deployment

Production deployment uses this exact annotated tag and verifies `/version` and `/health` against its commit identity.

## Known limitations

- Identity examples are provider-neutral boundaries, not a configured external tenant.
- WCAG and ISO references are alignment demonstrations, not certification claims.
- Billing data is synthetic.

## Previous release

None — first public release.

## Rollback target

None — first public release.
