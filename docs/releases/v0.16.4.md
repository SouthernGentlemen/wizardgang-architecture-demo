# WizardGang Architecture Demo v0.16.4

**Product:** WizardGang Architecture Demo

**Version:** v0.16.4

**Release date:** 2026-09-09

**Commit:** the commit this tag names — `git rev-list -n1 v0.16.4`

## Scope

Simplify the Platform laboratory surfaces and make the Workers and R2 actions
clear, functional demonstrations.

## Included changes

- DEMO-215: removed implementation-only D1 relationship, parameter, and binding panels, along with R2 storage-flow metadata and the Workers request-body disclosure.
- DEMO-215: replaced the unclear bounded arithmetic example with a stateless edge cache and routing policy action.
- DEMO-215: fixed R2 latest-operation tracking for downloads, removed duplicate inline download controls, added text and SVG previews, and exposed Delete beside Preview where ownership permits.

## Validation

The release candidate passed the complete repository check with 498 tests,
local migrations, the dependency audit with zero vulnerabilities, the Worker
dry-run build, and `git diff --check`. The exact annotated tag must reproduce
`npm ci`, `npm run check`, `npm run validate:migrations`,
`npm run security:dependencies`, and `npm run build` before deployment.

## Deployment

Target: `demo.wizardgang.ai`. The tag-triggered Release workflow reproduces the
annotated v0.16.4 state, publishes a release-bound assurance snapshot, deploys
the exact tag, and verifies public version/commit identity and Worker health.
Live acceptance additionally verifies the simplified Platform pages, Worker
edge-policy action, R2 download operation tracking, text preview, and SVG
preview.

## Known limitations

- Provider billing availability is independent of application health; unavailable billing is never estimated as real cost.
- Availability is measured observation history, not an SLA.
- Acceptance is not accessibility certification or an exhaustive cross-browser test.

## Previous release

v0.16.3

## Rollback target

v0.16.3
