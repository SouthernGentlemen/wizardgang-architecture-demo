# WizardGang Architecture Demo v0.17.0

**Product:** WizardGang Architecture Demo

**Version:** v0.17.0

**Release date:** 2026-09-09

**Commit:** the commit this tag names — `git rev-list -n1 v0.17.0`

## Scope

Release the assurance presentation rebuild and the corrected scheduled-observation availability history after all open pull requests were merged.

## Included changes

- DEMO-225: rebuild the public evidence presentation around a source-first catalog.
- DEMO-229: rebuild compliance as a framework-first instructional review with progressive record disclosure and resolvable exact-record links.
- DEMO-230: rebuild the risk presentation around current posture, priority exposure, filters, and evidence-backed records.
- DEMO-231: rebuild incident and exercise presentation without duplicating canonical reporting output.
- DEMO-232: rebuild public concern intake and its assurance presentation.
- DEMO-233: restrict availability history to scheduled observations, expose monitoring coverage, classify planned maintenance separately, and enforce 365-day retention.

## Validation

The release candidate passed the complete repository check with 528 tests across 90 test files, local migrations, the dependency audit with zero vulnerabilities, the Worker dry-run build, and `git diff --check`. The exact annotated tag must reproduce the same gates before deployment.

## Deployment

Target: `demo.wizardgang.ai`. The tag-triggered Release workflow reproduces the annotated v0.17.0 state, publishes a release-bound assurance snapshot, deploys the exact tag, and verifies public version/commit identity and Worker health.

## Known limitations

- Availability is scheduler-owned self-observation with explicit coverage reporting, not an independent external SLA monitor.
- Retained observations created before DEMO-233 remain in storage until normal expiry but are excluded from measured availability when they lack the scheduled-observation marker.
- Public assurance material is instructional engineering evidence and does not claim certification or exhaustive conformance.

## Previous release

v0.16.9

## Rollback target

v0.16.9
