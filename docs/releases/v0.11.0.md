# WizardGang Architecture Demo v0.11.0

**Product:** WizardGang Architecture Demo

**Version:** v0.11.0

**Release date:** 2026-09-02

**Commit:** the commit this tag names — `git rev-list -n1 v0.11.0`

## Scope

Make security disclosure and non-sensitive concern intake operational through private GitHub vulnerability reporting, public security-policy discovery, RFC 9116 `security.txt`, and structured public Issue Forms.

## Included changes

- [DEMO-108] [SEC] Establish concern and vulnerability intake.
- [DEMO-109] [BUILD] Release security and concern intake as v0.11.0.

## Validation

- Private vulnerability reporting is enabled and independently returned `enabled=true` through the GitHub repository API.
- The concern label exists and all Issue Form YAML parses successfully.
- The feature pull request passed required change-ID and validation checks before merge.
- The combined mainline passes 120 tests across 22 files, route/scaffold/history/governance/assurance validation, type checking, contracts, locales, security checks, D1 migration validation, dependency audit, and the Worker dry-run build.
- Release automation reproduces the annotated tag before deployment and verifies the live `/version` and `/health` surfaces afterward.

## Deployment

Target: `demo.wizardgang.ai`

The release workflow deploys only the reviewed annotated tag and publishes this record as the matching GitHub Release.

## Known limitations

- Public risk, incident, exercise, and security-advisory registries are not yet exposed.
- GitHub provides the submission system; native demo-hosted form persistence is intentionally deferred until it can include appropriate abuse controls and operator-approved promotion.
- A confirmed report does not automatically become an incident, GHSA, or CVE.
- Management-system and standards statements remain aligned and uncertified.

## Previous release

v0.10.0

## Rollback target

v0.10.0
