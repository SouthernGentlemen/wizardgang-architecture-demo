# WizardGang Architecture Demo v0.16.5

**Product:** WizardGang Architecture Demo

**Version:** v0.16.5

**Release date:** 2026-09-09

**Commit:** the commit this tag names — `git rev-list -n1 v0.16.5`

## Scope

Rebuild the public interface laboratory around focused, executable examples.

## Included changes

- DEMO-217: replaced the REST explorer with an anonymous OpenAPI 3.0-style CRUD contract and a visitor-scoped D1 demo API supporting GET, POST, PUT, PATCH, and DELETE without authorization headers.
- DEMO-217: added runnable public GraphQL examples, changed the webhook action to simulate a signed latest-release notification, and removed the requested architecture/reference panels from the interface pages.

## Validation

The release candidate passed the complete repository check with 500 tests,
local migrations, the dependency audit with zero vulnerabilities, the Worker
dry-run build, and `git diff --check`. The exact annotated tag must reproduce
`npm ci`, `npm run check`, `npm run validate:migrations`,
`npm run security:dependencies`, and `npm run build` before deployment.

## Deployment

Target: `demo.wizardgang.ai`. The tag-triggered Release workflow reproduces the
annotated v0.16.5 state, publishes a release-bound assurance snapshot, deploys
the exact tag, and verifies public version/commit identity and Worker health.
Live acceptance additionally verifies the anonymous REST CRUD operations,
GraphQL examples, release webhook action, and simplified interface pages.

## Known limitations

- Provider billing availability is independent of application health; unavailable billing is never estimated as real cost.
- Availability is measured observation history, not an SLA.
- Acceptance is not accessibility certification or an exhaustive cross-browser test.

## Previous release

v0.16.4

## Rollback target

v0.16.4
