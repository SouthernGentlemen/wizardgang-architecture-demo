# WizardGang Architecture Demo v0.16.1

**Product:** WizardGang Architecture Demo

**Version:** v0.16.1

**Release date:** 2026-09-08

**Commit:** the commit this tag names — `git rev-list -n1 v0.16.1`

## Scope

Restore the shared declarative demonstration controls that issue live API requests
and display their responses on platform pages.

## Included changes

- DEMO-209: escape the response separator through the server-side template so the
  emitted browser script remains valid, and compile all inline platform scripts in
  route regression tests.
- DEMO-210: align package metadata and release evidence for the v0.16.1 forward fix.

## Validation

The fix PR passed the complete repository check with 497 tests, local migrations,
the dependency audit with zero vulnerabilities, the Worker dry-run build, and
`git diff --check`. The exact annotated tag must reproduce `npm ci`,
`npm run check`, `npm run validate:migrations`, `npm run security:dependencies`,
and `npm run build` before deployment.

## Deployment

Target: `demo.wizardgang.ai`. The tag-triggered Release workflow reproduces the
annotated v0.16.1 state, publishes a release-bound assurance snapshot, deploys the
exact tag, and verifies public version/commit identity and Worker health. Live
acceptance additionally verifies that a shared platform action returns and renders
its API response without a browser syntax error.

## Known limitations

- Provider billing availability is independent of application health; unavailable billing is never estimated as real cost.
- Availability is measured observation history, not an SLA.
- Acceptance is not accessibility certification or an exhaustive cross-browser test.

## Previous release

v0.16.0

## Rollback target

v0.16.0
