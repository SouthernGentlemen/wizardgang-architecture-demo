# WizardGang Architecture Demo v0.16.7

**Product:** WizardGang Architecture Demo

**Version:** v0.16.7

**Release date:** 2026-09-09

**Commit:** the commit this tag names — `git rev-list -n1 v0.16.7`

## Scope

Restore the remaining live Cloudflare resource metrics on the operations usage
page after completing the production telemetry configuration.

## Included changes

- DEMO-222: query the current `durableObjectsStorageGroups` provider dataset.
- DEMO-222: align public-safe observation provenance with that provider dataset.
- Production configuration: add a dedicated account-scoped token limited to
  Account Analytics Read and Billing Read, and scope Durable Objects telemetry
  to the deployed `DemoCoordinator` namespace.

## Validation

The release candidate passed the complete repository check with 502 tests,
local migrations, the dependency audit with zero vulnerabilities, the Worker
dry-run build, and `git diff --check`. The exact annotated tag must reproduce
`npm ci`, `npm run check`, `npm run validate:migrations`,
`npm run security:dependencies`, and `npm run build` before deployment.

## Deployment

Target: `demo.wizardgang.ai`. The tag-triggered Release workflow reproduces the
annotated v0.16.7 state, publishes a release-bound assurance snapshot, deploys
the exact tag, and verifies public version/commit identity and Worker health.
Live acceptance verifies that Workers, D1, R2, and Durable Objects observations
are populated on `/operations/usage` and its shared reporting interface.

## Known limitations

- Cloudflare's provider billing API does not currently return a usable billed
  cost value for this account; the page reports that field as unavailable and
  does not substitute a local estimate.
- Availability is measured observation history, not an SLA.
- Acceptance is not accessibility certification or an exhaustive cross-browser test.

## Previous release

v0.16.6

## Rollback target

v0.16.6
