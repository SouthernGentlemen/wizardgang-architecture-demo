# WizardGang Architecture Demo v0.16.3

**Product:** WizardGang Architecture Demo

**Version:** v0.16.3

**Release date:** 2026-09-09

**Commit:** the commit this tag names — `git rev-list -n1 v0.16.3`

## Scope

Forward-fix the D1 confirmation flow so reset and delete requests finish cleanly
after their asynchronous API calls.

## Included changes

- DEMO-213: retain the confirmation button reference before awaiting the D1
  request, preventing the post-await `Event.currentTarget` null access that left
  the dialog open and the control disabled.

## Validation

The fix PR passed the complete repository check with 497 tests, local
migrations, the dependency audit with zero vulnerabilities, the Worker dry-run
build, and `git diff --check`. The exact annotated tag must reproduce `npm ci`,
`npm run check`, `npm run validate:migrations`, `npm run security:dependencies`,
and `npm run build` before deployment.

## Deployment

Target: `demo.wizardgang.ai`. The tag-triggered Release workflow reproduces the
annotated v0.16.3 state, publishes a release-bound assurance snapshot, deploys
the exact tag, and verifies public version/commit identity and Worker health.
Live acceptance additionally verifies that D1 confirmation actions close after
their API requests complete.

## Known limitations

- Provider billing availability is independent of application health; unavailable billing is never estimated as real cost.
- Availability is measured observation history, not an SLA.
- Acceptance is not accessibility certification or an exhaustive cross-browser test.

## Previous release

v0.16.2

## Rollback target

v0.16.2
