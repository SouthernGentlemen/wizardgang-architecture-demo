# WizardGang Architecture Demo v0.15.1

**Product:** WizardGang Architecture Demo

**Version:** v0.15.1

**Release date:** 2026-09-08

**Commit:** the commit this tag names — `git rev-list -n1 v0.15.1`

## Scope

Forward-correct the v0.15.0 release defect found during independent live verification. The canonical hierarchy remains unchanged; the generic router now rejects the last surviving retired query-state family, `/assurance?view=…`, as the ordinary 404 with no redirect.

The v0.15.0 tag is immutable and is not moved or deleted. v0.15.1 is the patch release that restores the documented invariant that every retired `?view=` selector is dead while retaining all canonical routes, APIs, protocols, generated artifacts, and assurance anchors.

## Included changes

- [DEMO-200] [FIX] Reject any legacy `view` query parameter before route dispatch, add coverage for all former assurance selectors, and align package metadata with v0.15.1.

## Validation

- The release pull request must pass `npm run check`, `npm run validate:migrations`, `npm run security:dependencies`, `npm run build`, and `git diff --check`.
- Exact-tag reproduction must pass the same suite before the v0.15.1 GitHub Release and deployment.
- Independent live verification must repeat the v0.15.0 matrix and confirm `/assurance?view={overview,delivery,governance,evidence,compliance,risks,incidents,concerns,unknown}` returns ordinary 404 responses with no `Location` header.

## Deployment

Target: `demo.wizardgang.ai`

The v0.15.1 release workflow deploys only the annotated v0.15.1 tag. It is the forward correction for v0.15.0; the v0.15.0 tag and deployment remain historically preserved.

## Live verification evidence

The GitHub Release copy of this note records the observed exact-tag deployment, version/commit identity, canonical route and sitemap matrix, legacy and retired-query 404 matrix, framework filters, protocol boundaries, assurance anchors, and laboratory smoke tests.

## Known limitations

- v0.15.0 remains published and immutable with the `/assurance?view=` defect documented in its live-verification evidence. Consumers should use v0.15.1 or later for the complete retired-query 404 contract.
- Public incident and advisory datasets remain empty; no fictional `INC-*` or `ADV-*` anchors are fabricated.

## Previous release

v0.15.0

## Rollback target

v0.14.2
